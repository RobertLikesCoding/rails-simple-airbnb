<div class="container d-flex justify-content-center">
  <div class="row">
    <h1 class="my-4 text-center">Edit this flat: <%= @flat.name %></h1>
    <div class="col-10 mx-auto">
      <div class="col-6 mx-auto">
        <%= render "form", flat: @flat %>
        <%= link_to "Delete Flat", flat_path(@flat), class: "text-danger", data: {turbo_method: "delete", turbo_confirm: "Are you sure?"} %>
      </div>
    </div>
  </div>
</div>


  </div>

</div>
